import 'package:flutter/material.dart';

class Fliegengitter extends StatelessWidget {
  const Fliegengitter({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
