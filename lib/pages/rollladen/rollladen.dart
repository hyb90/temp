import 'package:flutter/material.dart';

class Rollladen extends StatelessWidget {
  const Rollladen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
