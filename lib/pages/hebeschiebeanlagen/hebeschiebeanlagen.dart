import 'package:flutter/material.dart';

class Hebeschiebeanlagen extends StatelessWidget {
  const Hebeschiebeanlagen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
